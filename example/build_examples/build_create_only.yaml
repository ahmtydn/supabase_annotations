targets:
  $default:
    builders:
      supabase_annotations|schema_builder:
        enabled: true
        options:
          migration_mode: 'createOnly'  # Default mode - original behavior
          format_sql: true
          generate_comments: true
          enable_rls_by_default: false
        generate_for:
          - lib/**.dart
